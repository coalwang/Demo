package android;

public class DesignPattern {

    //单例
    /**
     第一种方式：枚举，线程安全，且只有一个实例
     public enum Person {
        INSTANCE;
        public Person getInstance() {
            return INSTANCE;
        }
     }
     */
}
